tests:
  hw_id.device_id:
    integration_platforms:
      - native_posix
    platform_allow: native_posix
    tags: ci_build
    extra_args: KCONFIG_OVERRIDE_FILE="kconfig_override_device_id.h"
  hw_id.uuid:
    integration_platforms:
      - native_posix
    platform_allow: native_posix
    tags: ci_build
    extra_args: KCONFIG_OVERRIDE_FILE="kconfig_override_uuid.h"
  hw_id.imei:
    integration_platforms:
      - native_posix
    platform_allow: native_posix
    tags: ci_build
    extra_args: KCONFIG_OVERRIDE_FILE="kconfig_override_imei.h"
  hw_id.ble:
    integration_platforms:
      - native_posix
    platform_allow: native_posix
    tags: ci_build
    extra_args: KCONFIG_OVERRIDE_FILE="kconfig_override_ble.h"
  hw_id.net:
    integration_platforms:
      - native_posix
    platform_allow: native_posix
    tags: ci_build
    extra_args: KCONFIG_OVERRIDE_FILE="kconfig_override_net.h"
