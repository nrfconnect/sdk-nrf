common:
  sysbuild: true
  build_only: true
  tags:
    - mcuboot

# Reduced code size for mcuboot for nRF54H20
# Incompatibility introduced in commit ae3449e

tests:
  mcuboot.backward_compatibility.dfu_multi_image:
    platform_allow:
      - nrf54h20dk/nrf54h20/cpuapp
