# Backward compatibility: v3.2 MCUboot on nrf54l15 occupies 80K (0x14000) so
# slot0 starts at 0x14800. The v3.3 default value (0x16000) would shift slot0
# to 0x16800 and the v3.2 bootloader on the device would jump to the wrong
# address, leaving the board in a reset loop after serial recovery upload.
# The v3.3 MCUboot binary (~68K) still fits in the 80K partition.
CONFIG_PM_PARTITION_SIZE_MCUBOOT=0x14000

# disable fprotect (to allow boot with bigger MCUboot)
CONFIG_FPROTECT=n
# MCUboot requires a large stack size, otherwise an MPU fault will occur
CONFIG_MAIN_STACK_SIZE=16384

# Enable flash operations
CONFIG_FLASH=y

CONFIG_BOOT_MAX_IMG_SECTORS=256

CONFIG_MULTITHREADING=y

CONFIG_LOG=y
CONFIG_LOG_MODE_MINIMAL=y
CONFIG_LOG_DEFAULT_LEVEL=0
CONFIG_MCUBOOT_LOG_LEVEL_INF=y
# Decrease footprint by ~4 KB in comparison to CBPRINTF_COMPLETE=y
CONFIG_CBPRINTF_NANO=y
# Use minimal C library instead of the Picolib
CONFIG_MINIMAL_LIBC=y

CONFIG_FW_INFO_FIRMWARE_VERSION=2

# NCS boot banner
CONFIG_NCS_APPLICATION_BOOT_BANNER_STRING="MCUboot"
