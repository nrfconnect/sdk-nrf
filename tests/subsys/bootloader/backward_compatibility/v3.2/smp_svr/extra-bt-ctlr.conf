# Companion of extra-bt.conf for boards that run the Bluetooth controller in the
# same image. BT_CTLR_DATA_LENGTH_MAX depends on BT_CTLR_DATA_LENGTH, which does
# not exist when the controller is reached over HCI IPC on another core.
CONFIG_BT_CTLR_DATA_LENGTH_MAX=251
