#
# Copyright (c) 2025 Nordic Semiconductor ASA
#
# SPDX-License-Identifier: LicenseRef-Nordic-5-Clause
#

# This file is only used by boot.kmu_keys_lock.smoke_test, which builds no
# bootloader at all. BOOT_SIGNATURE_KEY_FILE depends on BOOTLOADER_MCUBOOT and
# SECURE_BOOT_SIGNING_KEY_FILE depends on SECURE_BOOT, so both assignments were
# dropped here. The scenarios that do build a bootloader now pick up the key
# file from the matching sysbuild_<FILE_SUFFIX>.conf.
# SB_CONFIG_BOOT_SIGNATURE_KEY_FILE="\${APPLICATION_CONFIG_DIR}/private_key.pem"
# SB_CONFIG_SECURE_BOOT_SIGNING_KEY_FILE="\${APPLICATION_CONFIG_DIR}/private_key.pem"
