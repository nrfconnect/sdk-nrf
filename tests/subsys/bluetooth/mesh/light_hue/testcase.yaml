tests:
  bluetooth.mesh.light_hue:
    platform_allow: native_posix qemu_cortex_m3
    tags: bluetooth ci_build
    integration_platforms:
      - qemu_cortex_m3
  bluetooth.mesh.light_hue.sysbuild:
    sysbuild: true
    platform_allow: native_posix qemu_cortex_m3
    tags: bluetooth ci_build sysbuild
    integration_platforms:
      - qemu_cortex_m3
