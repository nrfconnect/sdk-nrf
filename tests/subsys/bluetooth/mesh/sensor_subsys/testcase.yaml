tests:
  bluetooth.mesh.sensor_subsys:
    platform_allow: native_posix qemu_cortex_m3
    tags: bluetooth ci_build
    integration_platforms:
        - native_posix
        - qemu_cortex_m3
  bluetooth.mesh.sensor_subsys.sysbuild:
    sysbuild: true
    platform_allow: native_posix qemu_cortex_m3
    tags: bluetooth ci_build sysbuild
    integration_platforms:
        - native_posix
        - qemu_cortex_m3
