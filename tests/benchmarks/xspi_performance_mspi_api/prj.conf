CONFIG_ZTEST=y
CONFIG_MSPI=y
# The SCK delay measurement drives its GPIO input directly through nrfx_gpiote,
# bypassing the Zephyr GPIO subsystem, so it is not needed here.
CONFIG_GPIO=n
CONFIG_CPU_LOAD=y
CONFIG_NRFX_TIMER=y
