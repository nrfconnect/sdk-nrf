common:
  tags:
    - drivers
    - mspi
    - ci_tests_drivers_mspi
  harness: ztest
  harness_config:
    fixture: mspi_tx_rx_loopback

tests:
  drivers.mspi.mspi_tx_rx_loopback:
    platform_allow:
      - nrf7120pdk/nrf7120/cpuapp
    extra_configs:
      - CONFIG_MSPI_DMA=y
