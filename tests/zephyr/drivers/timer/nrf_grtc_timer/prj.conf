CONFIG_ZTEST=y
CONFIG_NRF_GRTC_TIMER=y
CONFIG_COUNTER=y
CONFIG_TEST_RANDOM_GENERATOR=y
# Ineffective here: XOSHIRO_RANDOM_GENERATOR depends on ENTROPY_HAS_DRIVER, which none of the
# platforms this test is allowed on enables, so the RNG choice falls back to
# TIMER_RANDOM_GENERATOR that TEST_RANDOM_GENERATOR above already defaults to.
# CONFIG_XOSHIRO_RANDOM_GENERATOR=y
