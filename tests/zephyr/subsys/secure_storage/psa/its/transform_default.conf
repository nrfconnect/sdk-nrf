# Only for board targets with a hardware unique key. HW_UNIQUE_KEY is not
# available on native_sim, so the scenarios that run there use their own
# variants that leave this fragment out.
CONFIG_HW_UNIQUE_KEY_WRITE_ON_CRYPTO_INIT=y
