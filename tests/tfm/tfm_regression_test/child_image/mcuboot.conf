# When integration testing MCUBoot with this test we need to save as
# much space as possible, so drop support for having two slots to use
# during DFU.

CONFIG_SINGLE_APPLICATION_SLOT=y

# UPDATEABLE_IMAGE_NUMBER needed to be set to 1 for
# SINGLE_APPLICATION_SLOT to build for an unknown reason.
CONFIG_UPDATEABLE_IMAGE_NUMBER=1