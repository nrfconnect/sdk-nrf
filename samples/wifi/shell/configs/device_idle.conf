# Device PM configuration for idle states
# Reduces peripheral power consumption when system is idle

# System clocks and peripherals can power down in idle
# Keep only required clocks running for Wi-Fi and network
CONFIG_CLOCK_CONTROL=y

# Enable power domain control
CONFIG_PM_DEVICE=y
CONFIG_PM_DEVICE_RUNTIME=y
