# RAM power down configuration
# Powers down unused RAM blocks while retaining essential memory for Wi-Fi operations
# Requires RAM power down library support in Nordic NCS

CONFIG_RAM_POWER_DOWN_LIBRARY=y

# Keep essential RAM powered during idle
# This retains enough memory for:
# - Network stack (TCP/UDP, DHCP)
# - Wi-Fi driver state
# - Packet buffers
