# Wi-Fi idle state optimization
# Keeps Wi-Fi radio available for scan/connect while minimizing power in idle

# Reduce network event queue processing when idle
CONFIG_NET_MGMT_EVENT_QUEUE_TIMEOUT=5000

# Network stack buffer optimization for idle
CONFIG_NET_BUF_RX_COUNT=8
CONFIG_NET_BUF_TX_COUNT=8
