# Applied only by the scenarios that run the Bluetooth controller in this image.
# BT_CTLR_DATA_LENGTH_MAX depends on BT_CTLR_DATA_LENGTH, which does not exist on
# the nRF5340 application core, where the controller lives on the network core.
CONFIG_BT_CTLR_DATA_LENGTH_MAX=251
