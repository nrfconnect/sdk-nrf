# Applied only by the scenarios that run the Bluetooth controller in this image.
# BT_CTLR_DATA_LENGTH_MAX depends on BT_CTLR_DATA_LENGTH, which does not exist in
# the multicore builds where the controller lives on the radio core.
CONFIG_BT_CTLR_DATA_LENGTH_MAX=251
