#
# Copyright (c) 2019 Nordic Semiconductor ASA
#
# SPDX-License-Identifier: LicenseRef-Nordic-5-Clause
#

# TF-M config. Only the ``*/ns`` board targets build TF-M, so the TFM_PROFILE
# choice does not exist for board targets that run without it.
CONFIG_TFM_PROFILE_TYPE_MINIMAL=y
