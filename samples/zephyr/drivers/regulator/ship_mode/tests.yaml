sample:
  description: Enter ship mode using regulator parent
  name: Regulator parent ship mode
common:
  harness: console
  harness_config:
    type: one_line
    regex:
      - "(.*) entering ship mode"
  tags:
    - ci_samples_zephyr_drivers_regulator

tests:
  sample.drivers.regulator.ship_mode:
    platform_allow:
      - nrf54lv10dk/nrf54lv10a/cpuapp
