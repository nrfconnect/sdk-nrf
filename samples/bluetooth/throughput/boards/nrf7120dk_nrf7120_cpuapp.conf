# The Wi-Fi core's HFXO bring-up (hfxo64m_setup/hfxo64m_start) is only run at
# boot when this is enabled. It is required even for BLE-only use on DKs in
# LCS Production, otherwise MPSL can assert with "0x54ad HFXO failure".
CONFIG_SOC_NRF71_WIFI_BOOT=y
