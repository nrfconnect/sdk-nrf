paths-ignore:
  # Example/placeholder private keys for the manufacturing_app sample.
  # These are intentionally fake keys derived from trivial seeds (0x01*32,
  # 0x02*32) and are safe to commit. They are never used in production.
  - "samples/manufacturing_app/keys/private/**"
